{
  "name": "canvas-live-for-chatgpt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only Canvas LMS live context bridge for ChatGPT via a Chrome extension.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/background.js && node --check src/canvas-client.js && node --check src/router.js && node --check src/content.js && node --check popup/popup.js && node --check options/options.js"
  },
  "engines": {"node": ">=20"},
  "license": "MIT"
}