{
  "name": "canvas-live-for-chatgpt",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Read-only Canvas LMS live context bridge for ChatGPT via a Chrome extension.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "build": "node scripts/build-vendor.mjs",
    "scan-secrets": "node scripts/scan-secrets.mjs",
    "test:browser": "node scripts/browser-smoke.mjs && node scripts/composer-smoke.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "0.28.2",
    "fflate": "0.8.3",
    "linkedom": "0.18.13",
    "pdfjs-dist": "6.3.289"
  }
}
